# 2026 年 2 月 2 日 工作日誌 (最終版)

**1. 溝通模式確認與記憶更新**
*   確認未來溝通語言為**中文**，並將此偏好記錄至 `USER.md` 和 `MEMORY.md`。
*   澄清了我的長期與短期記憶機制，並建立了「**記憶衝突時請告知**」的核心原則。

**2. AI 老闆助理模板專案（M5 里程碑）**
*   確認您已完成 M5 里程碑，建立了 `USER.md`、`TOOLS.md` 和 `HEARTBEAT.md` 等個人化設定範例檔案。
*   **專案成果已上傳至 GitHub: [https://github.com/jacky6658/AIboss-assistant-for-clawdbot](https://github.com/jacky6658/AIboss-assistant-for-clawdbot)**。
*   我對 `templates` 資料夾結構進行了檢查。
*   我閱讀並學習了 `templates/agent-persona` 內的 `PERSONA.md`、`COMMUNICATION.md`、`WORKFLOW.md` 和 `RULES.md` 等檔案。
*   確認了「AI 老闆助理技能書」是以純文字檔案形式讓 AI 學習的。

**3. Clawdbot Gateway 定時任務教學**
*   提供了 Clawdbot `cron` 工具的使用教學，包括 `list`, `add`, `remove` 等指令。
*   指導您在 Gateway WebUI 中設定定時任務，例如「每日待辦檢查提醒」（設定為每天早上 9 點）。
*   解釋了 WebUI 中「Every」、「At」和「Cron」排程選項的區別。

**4. 模型管理與切換**
*   確認了您 `clawdbot.json` 中已配置的多種模型（Gemini, GPT, Claude 系列）。
*   嘗試將我目前的會話模型切換到 `claude-sonnet-4-5-thinking`，但因「is not allowed」錯誤失敗。
*   您嘗試手動重啟 Gateway 以應用更改，但 Gateway 服務未能正常重啟。
*   最終，您決定暫時使用目前的 `google-gemini-cli/gemini-2.5-flash` 模型。

**5. `USER.md` 檔案的討論與更新**
*   我糾正了之前對 `USER.md` 內容的錯誤記憶，並成功更新了檔案。
*   我們重新討論並更新了 `USER.md`，加入了您對我的「**對我的期待與規則**」區塊，整合了 `docs/rules.md` 中的核心原則和重要約定。
*   您提供了詳細的「**常用群組 ID**」資訊（包括公事與私人群組，以及其下的話題 ID），我已將其添加至 `USER.md` 中。
*   我們將我每次會話啟動的核心原則更新到了 `AGENTS.md`：**優先讀取 `USER.md`，然後掃描 `skills`，再進行溝通**。

**6. YouTube 摘要技能 (youtube-summarize) 整合**
*   您提供了 `yt-summarize-1.0.0.zip` 檔案，我將其解壓縮並將 `SKILL.md` 移動到 `skills/yt-summarize/` 目錄。
*   成功安裝了 `yt-dlp` 工具（透過 Homebrew）。
*   嘗試摘要您提供的 YouTube 影片，最終成功提取了中文字幕並提供了摘要。

---

**明日工作計畫**：
*   討論「新的商案開發」。
*   討論「提升 Clawdbot 穩定性的方式」。

**本週計畫**：無其他額外計畫。