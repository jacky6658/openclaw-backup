# 交接_狀態總覽（給 Jacky 確認用）

> 原則：這份檔案永遠保持「短」，只放：正在做 / 下一步 / Blocker / 已完成(最近)

## 正在做（In Progress）
- skills：desktop-control → 做成「可接到 OpenClaw 可直接用」的交付形態（依賴/權限/最小 demo/封裝方式）
- 測試機（`newuser@192.168.88.237`）收尾：確認 Firewall 開啟且 SSH 仍可連
- 章節二｜單元一：B 尚待你提供「不行時間點＋期望畫面」；D/E 未做
- PressPlay（PPA）內容：章節五待補
- JackyAI 網站課程價格改 1980：尚未完成

## 下一步（Next）
1) desktop-control：定稿 OpenClaw 整合方案（A: 納入 skills 目錄統一入口 / B: 獨立 skill package）+ 在測試機做 smoke test
2) 測試機：若你能在本機 Terminal 跑一次 sudo（下面指令）我再 SSH 驗證：
   - `sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate on`
   - `sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate`
   - `sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getstealthmode`
3) 章節二｜單元一：等你回 B 的具體修改點後，做 B 定稿 → 推 D/E
4) PPA：補完章節五文案

## Blocker
- /new 新對話會少即時上下文 → 需靠本檔 + MEMORY.md/待辦清單維持連續
- 影片 D.mov 轉寫曾遇 0 bytes（需替代轉寫方案）
- Tauri 開發環境注意：shell 若未載入 `~/.cargo/env`，會找不到 `cargo/rustc`（PATH 需含 `~/.cargo/bin`）

## 最近完成（Latest Done）
- OpenClaw Setup（Tauri）：已可打包出 `.app` + 用穩定 `hdiutil create` 產出 `.dmg`；新手安裝教學已補齊於 `installers/ui-openclaw-setup/README.md`
- desktop-control（skills-import）：已改成可 import 的 Python package 結構、venv 安裝流程 + 權限說明已寫進 README；並已清掉「誤 commit 的 .venv / __pycache__」(local commits: `6d6e4d1` + `4464561`)

## 固定規則（不要忘）
- 每日匯報：先給 Jacky 確認 OK 才能發
- 省 token：只回 1–3 行，除非你說「貼全文」
